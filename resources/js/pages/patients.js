/**
 * Patients Page Entry Point
 * Loads the unified patients module and initializes it
 */

import { initializePatientsPage } from '../shared/pages/patients.js';

// Module auto-initializes on DOMContentLoaded via the patients module
// This file just ensures the module is loaded via Vite
